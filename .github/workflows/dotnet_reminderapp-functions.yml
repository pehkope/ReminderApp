# Docs for the Azure Web Apps Deploy action: https://github.com/azure/functions-action
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy .NET project to Azure Function App - reminderapp-functions

on:
  push:
    branches:
      - main
  workflow_dispatch:

env:
  AZURE_FUNCTIONAPP_PACKAGE_PATH: './ReminderApp.Functions' # set this to the path to your function app project
  DOTNET_VERSION: '8.0.x' # set this to the dotnet version to use

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: 'Checkout GitHub Action'
        uses: actions/checkout@v4

      - name: Setup .NET ${{ env.DOTNET_VERSION }} Environment
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: 'Restore dependencies'
        run: |
          echo "📦 Restoring .NET dependencies..."
          dotnet restore "${{ env.AZURE_FUNCTIONAPP_PACKAGE_PATH }}"
          echo "✅ Dependencies restored"

      - name: 'Build project'
        run: |
          echo "🔨 Building .NET project..."
          dotnet build "${{ env.AZURE_FUNCTIONAPP_PACKAGE_PATH }}" --configuration Release --no-restore
          echo "✅ Build completed"

      - name: 'Publish project'
        run: |
          echo "📦 Publishing .NET project..."
          dotnet publish "${{ env.AZURE_FUNCTIONAPP_PACKAGE_PATH }}" --configuration Release --no-build --output ./publish
          echo "✅ Publish completed"

      - name: 'List deployment files'
        run: |
          echo "📁 Files to deploy:"
          ls -la ./publish
          echo "📄 Function files:"
          find ./publish -name "*.json" -o -name "*.dll" | head -10

      - name: 'Deploy to Azure Functions'
        uses: Azure/functions-action@v1
        with:
          app-name: 'reminderapp-functions'
          slot-name: 'Production'
          package: './publish'
          publish-profile: ${{ secrets.AZUREAPPSERVICE_PUBLISHPROFILE_D4D1DD18FDFB4E5DAA3C7FD52B4EB42C }}
