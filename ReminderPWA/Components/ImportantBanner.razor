<div class="@GetBannerClass()">
    @if (!string.IsNullOrWhiteSpace(ImportantMessage))
    {
        @ImportantMessage
    }
    else
    {
        <span>📅 Ei sovittuja tapaamisia</span>
    }
</div>

@code {
    [Parameter] public string? ImportantMessage { get; set; }
    
    private string GetBannerClass()
    {
        return !string.IsNullOrWhiteSpace(ImportantMessage) 
            ? "important-banner" 
            : "no-appointments-banner";
    }
}