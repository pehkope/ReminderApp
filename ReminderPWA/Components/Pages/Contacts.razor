@page "/contacts"
@using ReminderTabletNew2.Services
@inject ApiService ApiService
@using ReminderTabletNew2.Models
@inject AppConfig Config
@using System.Text.Json
@inject NavigationManager Navigation
@inject Microsoft.JSInterop.IJSRuntime JS

<SwipeContainer OnSwipe="HandleSwipe" CssClass="contacts-swipe-container">
    <div class="contacts-page">
        <div class="header">
            <h2>📞 SOITA PERHE</h2>
            <button class="back-button" @onclick="GoBack">← Takaisin</button>
        </div>
        
        <div class="contacts-content">
        @if (apiResponse?.Contacts?.Any() == true)
        {
            @foreach (var contact in apiResponse.Contacts)
            {
                <div class="contact-item">
                    @if (!string.IsNullOrEmpty(contact.PhotoUrl))
                    {
                        <div class="contact-photo">
                            <img src="@contact.PhotoUrl" alt="@contact.Name" 
                                 @onload="@(() => Console.WriteLine($"Contact photo loaded: {contact.Name}"))"
                                 @onerror="@(() => Console.WriteLine($"Contact photo failed: {contact.Name}"))" />
                        </div>
                    }
                    <div class="contact-info">
                        <div class="name">@contact.Name @if(!string.IsNullOrEmpty(contact.TelegramChatID)){<span class="tg-badge">TG</span>}</div>
                        <div class="phone">📱 @contact.Phone</div>
                        @if (!string.IsNullOrEmpty(contact.Relationship))
                        {
                            <div class="relationship">@contact.Relationship</div>
                        }
                    </div>
                    @if (!string.IsNullOrWhiteSpace(contact.Phone))
                    {
                        <button class="call-button" @onclick="() => OnCall(contact.Phone)" disabled="@(!twilioReady)">
                            📞 Soita
                        </button>
                        @if (!twilioReady)
                        {
                            <span class="call-hint">Salli mikrofoni ja varmista Twilio-token</span>
                        }
                    }
                    else
                    {
                        <button class="call-button" disabled>
                            📞 Soita
                        </button>
                    }
                    <button class="msg-button" @onclick="() => MessageContact(contact)" disabled="@string.IsNullOrEmpty(contact.TelegramChatID)">
                        💬 Viesti
                    </button>
                </div>
            }
        }
        else
        {
            <div class="no-contacts">
                <p>Yhteystietoja ei löytynyt.</p>
                <p>Tarkista Google Sheets -asetukset.</p>
            </div>
        }
        </div>
    </div>
</SwipeContainer>

@code {
    private ReminderApiResponse? apiResponse;
    private bool twilioReady = false;
    private string? callStatus;

    protected override async Task OnInitializedAsync()
    {
        await LoadData();
    }

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            try
            {
                await JS.InvokeVoidAsync("twilioVoice.init", Config.Twilio.TokenUrl, Config.Twilio.DefaultIdentity);
                twilioReady = true;
                StateHasChanged();
            }
            catch (Exception ex)
            {
                twilioReady = false;
                callStatus = $"Twilio ei alustunut: {ex.Message}";
                StateHasChanged();
            }
        }
    }

    private async Task LoadData()
    {
        try
        {
            var result = await ApiService.GetDataAsync("mom", maxRetries: 1, timeoutOverrideSeconds: 6);
            if (result.IsSuccess)
            {
                apiResponse = result.Data;
            }
            else
            {
                Console.WriteLine($"Contacts API error: {result.ErrorCode} - {result.ErrorMessage}");
                // Fallback emergency contacts when API returns failure without exception
                apiResponse = new ReminderApiResponse
                {
                    Contacts = new List<Contact>
                    {
                        new Contact { Name = "Hätänumero", Phone = "112" },
                        new Contact { Name = "Perhe (varanumero)", Phone = "+358401234567" }
                    }
                };
            }
            
            // If no contacts from API, add temporary test contacts
            if (apiResponse?.Contacts?.Any() != true)
            {
                Console.WriteLine("=== NO CONTACTS FOUND ===");
                
                if (apiResponse == null)
                    apiResponse = new ReminderApiResponse();
                    
                apiResponse.Contacts = new List<Contact>
                {
                    new Contact { Name = "Petri (poika)", Phone = "+358401234567" },
                    new Contact { Name = "Tiita (tytär)", Phone = "+358501234568" },
                    new Contact { Name = "Sisko (sisar)", Phone = "+358451234569" },
                    new Contact { Name = "Ystävä Liisa", Phone = "+358401234570" },
                    new Contact { Name = "Naapuri Marja", Phone = "+358501234571" },
                    new Contact { Name = "Hätänumero", Phone = "112" }
                };
                
                Console.WriteLine("Added temporary test contacts");
            }

            // Lisää Petrin testinumero varmistetusti listaan, jos puuttuu
            try
            {
                const string testNumber = "+358400607737";
                var hasNumber = apiResponse?.Contacts?.Any(c => string.Equals(c.Phone, testNumber, StringComparison.OrdinalIgnoreCase)) == true;
                if (apiResponse != null && apiResponse.Contacts != null && !hasNumber)
                {
                    apiResponse.Contacts.Insert(0, new Contact { Name = "Petri (testi)", Phone = testNumber, Relationship = "Poika" });
                    Console.WriteLine("Inserted Petri test contact for quick call testing");
                }
            }
            catch {}
        }
        catch (Exception ex)
        {
            Console.WriteLine($"Error loading data: {ex.Message}");
            
            // Create emergency fallback
            apiResponse = new ReminderApiResponse
            {
                Contacts = new List<Contact>
                {
                    new Contact { Name = "Hätänumero", Phone = "112" },
                    new Contact { Name = "Perhe", Phone = "+358401234567" }
                }
            };
        }
    }

    private async Task OnCall(string phone)
    {
        try
        {
            if (!twilioReady)
            {
                // Fallback puhelinlinkki jos Twilio ei ole valmis
                await JS.InvokeVoidAsync("window.open", $"tel:{phone}", "_self");
                return;
            }
            await JS.InvokeVoidAsync("twilioVoice.call", phone);
            callStatus = $"Soitetaan: {phone}";
        }
        catch (Exception ex)
        {
            callStatus = $"Virhe soitossa: {ex.Message}";
        }
    }

    private async Task MessageContact(Contact contact)
    {
        // Avaa TelegramSender suoraan tälle contactille
        await JS.InvokeVoidAsync("localStorage.setItem", "prefillTelegramChatId", contact.TelegramChatID ?? "");
        Navigation.NavigateTo("/", forceLoad: false);
    }

    private void GoBack()
    {
        Navigation.NavigateTo("/");
    }

    private Task HandleSwipe(string direction)
    {
        if (direction == "left")
        {
            // Left swipe - go back to main page
            GoBack();
        }
        return Task.CompletedTask;
    }
} 